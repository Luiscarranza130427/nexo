# pnpm configuration — Nexo monorepo

# Resolve local workspace packages from the workspace instead of the registry.
# pnpm 10 flipped this default to false; the monorepo relies on it being on.
link-workspace-packages=true
prefer-workspace-packages=true
